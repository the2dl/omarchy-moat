# Handoff — 2026-09-09, second session

Continues `HANDOFF-2026-09-09.md`. That document's traps all still apply; read
it first. This one says which of its items are closed and what replaced them.

## Do this first

**Installed is 140. HEAD builds 142.** Nothing in this session is running yet.

```
sudo pacman -U ~/Projects/omarchy-moat/pkg/omarchy-moat-0.1.0-142-x86_64.pkg.tar.zst
sudo systemctl restart tetragon moatd
```

The **tetragon** restart matters: the udevadm fix below is Tetragon config and
is inert without it. Until then this machine still SIGKILLs udev workers on
every Logitech HID hotplug.

**Expect `moat-x-binary-modified` for `/usr/bin/powerprofilesctl` on the first
alert that touches it.** That is real and is described below, not a false
positive to tune away.

## Closed from the previous handoff

* **6, provenance never verified file contents.** `mtree.rs` reads pacman's own
  per-file sha256; `official` now requires the bytes to still match, and
  `moat-x-binary-modified` says so out loud when they do not.
* **2, PID reuse.** `util::PidFd` pins the process; open, verify, signal
  through the handle.
* **3, `find_index` conflated parse failure with absence.** Three answers now.
* **4, `/proc/self/exe` for Spotify.** Anchored with `parent`.
* **7, two dead tests.** Both lacked `#[test]`; both now run and pass.
* The build is warning-free, from eleven.

## What is NOT closed

1. **The building-block question is still a decision, not a measurement.**
   `moatctl neighbours` now takes the per-rule cross-family neighbour rate over
   the whole store, so the measurement is one command. It has still not been
   taken over a representative window: the only reading so far is 28 minutes of
   this repo's own `cargo test` runs, where detection-tier was 0.4% (matching
   the previous handoff) but signal-tier read 7.5% against its 28.8%. **Leave
   moat running for a normal week and re-run it before deciding anything.** The
   rules that would be switched off by gating read 0.0% even in that small
   sample: `cred-etc-shadow-read` over 287 rows, `persist-agent-config-write`
   over 155, `rootkit-evidence-tamper` over 24.

2. ~~`/usr/bin/powerprofilesctl` is unexplained.~~ **Explained: Omarchy does
   it.** `install/config/fix-powerprofilesctl-shebang.sh` rewrites
   `#!/usr/bin/env python3` to `#!/bin/python3` so it uses the system python3
   and not mise's shim — exactly the 8-byte difference. Benign, and it means
   EVERY Omarchy machine has one permanently modified package file. That
   exposed a real flaw in the new rule, now fixed: the reported-set was held in
   memory, so this was a `high` alert on every restart. It is persisted, so the
   claim is once per change. Deliberately NOT allowlisted, with the reasoning
   in `default.toml` — an entry would excuse any future modification to a
   root-installed script the user runs. `ghc-libs`' `package.conf.d/package.cache`
   is the other mismatch on this machine and is a regenerated cache.

3. **No rule fires on a modified binary that never otherwise acts.** The check
   runs while classifying an actor, which means something has to alert about
   the file before its bytes are ever read. A periodic sweep of package-owned
   binaries would close that, and would cost a full-machine hash: 60,920 files
   under /usr/bin and /usr/lib here.

4. **`cargo clippy` has 25 warnings.** Not in the build (`makepkg` runs
   `cargo build` and `cargo test`, not clippy), so this is tidying. Ten are
   `doc list item without indentation`, and `--fix` on those touches a lot of
   comment blocks in a codebase where the comments carry the incident history.
   Read that diff, do not let it run unattended.

5. **azure-cli, item 5, is unchanged and still deliberate.** Do not "fix" it
   with a path glob; the reasoning is in `default.toml`.

6. **OPR submission, item 8**, untouched.

## What this session's work assumes

**`moat-x-binary-modified` trusts pacman's database.** The mtree is written at
install time and owned by root, so an attacker who can rewrite it can rewrite
`desc` beside it. This raises the bar from "replace a binary" to "replace a
binary and its checksum"; it is not a defence against root.

**The verification cache is keyed on cheap metadata** — path, inode, mtime,
size. A file rewritten in place at the same size with its mtime restored keeps
its key and its cached verdict until the next restart. That evades the cache,
not the check.

**Absence is never a mismatch.** No mtree, no digest for the path, a directory,
a symlink, or a file over the 512MB read cap all mean "no claim". The mutation
that treats any of them as "changed" fails six tests, three in the engine.
Do not change that direction without reading why.

## Method notes, because two of them cost real time here

**A guard is not checked until the mutation actually reproduces the bug.**
Twice this session a mutation passed and the guard looked fine:

* reverting the udevadm policy fix left the symlink scan passing, because the
  patch had REPLACED the dead entry rather than adding beside it, so there was
  no symlink left to find. Restored the original from git and re-checked.
* the first pidfd test passed with the pidfd swapped back for `libc::kill`. A
  reaped pid gives ESRCH either way. Only a genuinely recycled pid separates
  them, which needs a PID namespace and `/proc/sys/kernel/ns_last_pid`.

**Measure before building on it.** The mtree work was worth doing because
60,915 of 60,920 files matched. Had that number been 90%, the feature would
have been a noise generator, and nothing in the design would have said so.

**`git stash` plus cargo gives unreliable before/after test counts.** Twice it
served a stale binary and reported identical numbers for different trees. Use
the compiler's own dead-code warnings, or `git show HEAD:<file>`, to establish
what was there before.
