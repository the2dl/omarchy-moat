apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: sentinel-cred-vcs-token-read
  annotations:
    sentinel.omarchy/severity: "high"
    sentinel.omarchy/title: "Git forge token or .netrc read by an unexpected program"
    sentinel.omarchy/rotate: "github-token,git-credentials"
    sentinel.omarchy/enforce: "none"
    sentinel.omarchy/actions: "kill,quarantine,ignore"
    sentinel.omarchy/why: >-
      A GitHub token in gh's hosts.yml, ~/.git-credentials or ~/.netrc lets an attacker
      push to every repository you can push to; supply-chain worms use exactly this to
      spread from one developer to their organisation.
    sentinel.omarchy/expected: >-
      git and its credential helpers, gh, glab, curl invocations you started yourself
      that use .netrc, and IDE git plugins.
    sentinel.omarchy/fp-hint: "exe"
spec:
  options:
  - name: "policy-mode"
    value: "monitor"
  lsmhooks:
  - hook: "file_post_open"
    message: "Git forge credential file opened for reading"
    args:
    - index: 0
      type: "file"
    - index: 1
      type: "int"
    selectors:
    - matchBinaries:
      - operator: "NotPostfix"
        values:
        - "/git"
        - "/git-remote-http"
        - "/git-remote-https"
        - "/git-credential-store"
        - "/git-credential-libsecret"
        - "/git-credential-netrc"
        - "/gh"
        - "/glab"
        - "/tea"
        - "/hub"
        - "/lazygit"
      matchArgs:
      - index: 0
        operator: "Equal"
        values:
        - "{{HOME}}/.git-credentials"
        - "{{HOME}}/.netrc"
        - "{{HOME}}/.config/gh/hosts.yml"
        - "{{HOME}}/.config/glab-cli/config.yml"
        - "{{HOME}}/.config/git/credentials"
      - index: 1
        operator: "Mask"
        values: ["4"]
      matchActions:
      - action: Post
        rateLimit: "60s"
        rateLimitScope: "process"
