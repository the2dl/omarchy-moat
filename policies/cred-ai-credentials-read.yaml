apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: sentinel-cred-ai-credentials-read
  annotations:
    sentinel.omarchy/severity: "high"
    sentinel.omarchy/title: "AI CLI credential file read by an unexpected program"
    sentinel.omarchy/rotate: "anthropic-token,openai-token,google-token"
    sentinel.omarchy/enforce: "none"
    sentinel.omarchy/actions: "kill,quarantine,ignore"
    sentinel.omarchy/why: >-
      The OAuth tokens and API keys the AI CLIs cache buy an attacker your model quota
      and, worse, any repository or tool the agent is configured to reach; they are
      plain JSON at a fixed path.
    sentinel.omarchy/expected: >-
      claude, codex, gemini, opencode, q and amp themselves, and any node or bun process
      that is actually one of those CLIs.
    sentinel.omarchy/fp-hint: "exe"
spec:
  options:
  - name: "policy-mode"
    value: "monitor"
  lsmhooks:
  - hook: "file_post_open"
    message: "AI CLI credential file opened for reading"
    args:
    - index: 0
      type: "file"
    - index: 1
      type: "int"
    selectors:
    - matchBinaries:
      - operator: "NotPostfix"
        values:
        - "/node"
        - "/bun"
        - "/claude"
        - "/codex"
        - "/gemini"
        - "/opencode"
        - "/amp"
        - "/q"
        - "/qterm"
      matchArgs:
      - index: 0
        operator: "Equal"
        values:
        - "{{HOME}}/.claude/.credentials.json"
        - "{{HOME}}/.claude.json"
        - "{{HOME}}/.codex/auth.json"
        - "{{HOME}}/.config/openai/auth.json"
        - "{{HOME}}/.gemini/oauth_creds.json"
        - "{{HOME}}/.config/opencode/auth.json"
        - "{{HOME}}/.local/share/amazon-q/credentials.json"
      - index: 1
        operator: "Mask"
        values: ["4"]
      matchActions:
      - action: Post
        rateLimit: "60s"
        rateLimitScope: "process"
