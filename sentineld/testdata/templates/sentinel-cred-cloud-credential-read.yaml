apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: sentinel-cred-cloud-credential-read
  annotations:
    sentinel.omarchy/severity: high
    sentinel.omarchy/title: "Cloud or CLI credential file read"
    sentinel.omarchy/rotate: "aws,gh,npm-token"
    sentinel.omarchy/enforce: "none"
    sentinel.omarchy/actions: "kill,quarantine"
    sentinel.omarchy/why: >-
      Long-lived cloud and registry tokens sit in plain files; a build step that reads them
      is either exfiltrating or about to.
    sentinel.omarchy/expected: >-
      The matching CLI itself (aws, gh, npm), and credential helpers you installed.
    sentinel.omarchy/fp-hint: "exe+file"
spec:
  options:
  - name: policy-mode
    value: monitor
  kprobes:
  - call: "security_file_permission"
    syscall: false
    return: true
    args:
    - index: 0
      type: "file"
    - index: 1
      type: "int"
    returnArg:
      index: 0
      type: "int"
    selectors:
    - matchArgs:
      - index: 0
        operator: "Equal"
        values:
        - "{{HOME}}/.aws/credentials"
        - "{{HOME}}/.config/gh/hosts.yml"
        - "{{HOME}}/.npmrc"
      - index: 1
        operator: "Equal"
        values:
        - "4"
